# Editor temp files
.*.swp
*.swp
*~

# Go build output
/terraform-provider/terraform-provider-gigamon
/terraform-provider/terraform-provider-gigamon.exe
/tf_fm_backend/tf_fm_backend
/tf_fm_backend/tf_fm_backend.exe

# Release artifacts
artifacts/
dist/
*.zip
*.tar.gz
*.sig
*SHA256SUMS*
*_manifest.json

# Never commit signing material
*.gpg
*.asc
gpg_keys/
secring.*
pubring.*
trustdb.gpg
gpg_errors

# Terraform local state (must never be committed)
**/.terraform/
**/.terraform.lock.hcl
**/terraform.tfstate
**/terraform.tfstate.*
**/*.tfstate
**/*.tfstate.*
**/.terraform.tfstate.lock.info
**/*.tfvars
**/*.tfvars.json

# Local example dev iterations
**/*.tf.bak
**/*.tf.orig
**/*.tf.new

# OS / IDE
.DS_Store
.idea/
.vscode/
