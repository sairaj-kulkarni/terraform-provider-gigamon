# use Nginx to server out code-coverage log files

server {
	listen 80 default_server;
	listen [::]:80 default_server;

	root /code-coverage/logs;

	server_name _;

	location / {
        autoindex on;
	}

}

